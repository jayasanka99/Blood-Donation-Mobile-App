import 'package:flutter/material.dart';

class BLOOD_CAMP extends StatelessWidget {
  const BLOOD_CAMP({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(
          backgroundColor:  Color.fromARGB(200, 200, 0, 0),
          title: Text("BLOOD CAMP"),
        ),
    );
  }
}
